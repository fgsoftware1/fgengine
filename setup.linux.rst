.. code-block:: bash

	sudo apt update
	sudo apt install gcc-13 g++-13
	cd tools/xmake
	./configure
	sudo make install

Now you should be able to run xmake

.. code-block:: bash

	cd ..
	xmake